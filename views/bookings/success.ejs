<% layout("/layouts/boilerplate.ejs") %>

<div class="row mt-3">
    <h2>Payment Successful!</h2>
    <p>Your booking for <%= booking.listing.title %> is confirmed.</p>
    <p>Booking ID: <%= booking._id %></p>
    <p>Check-in: <%= booking.checkInDate.toLocaleDateString() %></p>
    <p>Check-out: <%= booking.checkOutDate.toLocaleDateString() %></p>
    <p>Total Price: ₹<%= booking.totalPrice %></p>
</div>